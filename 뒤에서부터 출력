s = input()

result = []

for i in range(0, len(s) + 1):
    result.append(s[len(s) - i:])
    
result.sort()
for r in result:
    print(r)
    

이거 c언어에서 너무 어려워서 못했던 것 같은데
여기서는 쉽네
여기서 for 반복문에서 괄호안에 지금까지 왜 이게 들어가는지 이해가 안 됐던게 이해가 됐다!
(0부터 len(s) + 1까지라는게 이 마지막 전까지인 거임 < 인거지 <=이게 아니었음!!

그리고 슬라이스 :이거를 앞에다 쓰는 거랑 이거를: 뒤에다 쓰는게 다른데 :이게 무슨 일을 하는지 공부해야겠다.

그리고 파이썬에서 배열을 지정할때는 = 이거를 써야 한다.

s[len(s) - 0:] 일때는 공백이 나왔다. 이 말은 s[5:]이면 아무것도 출력되지 않고
s[4:]이면 e가 출력된다. 
s[3:]이면 ce가 출력된다. 
이말은 아! 앞에 5개를 띄고 출력해라
앞에 4개를 건너뛰고 출력해라
이렇게 되는 건가보다!! 오케이
