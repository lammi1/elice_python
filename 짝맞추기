values = [int(v) for v in input().split()]
count = [0] * 10

for v in values:
    count[v] += 1
for i in range(len(count)):
    if count[i] == 1:
        print(i)


for v in values는 처음 본다. c언어에서는 for(int i = 0; i < values.size(); i++)
이런식으로 했던 것 같은데 배열의 크기구하는 함수를 까먹었네

파이썬에서는 배열의 크기 구하는 방법을 len(count)이렇게 구한다.

그리고 배열의 input을 받을 때 쓰는 코드도 처음 봤다.
