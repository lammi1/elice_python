def cross_bridge(steps):
    cnt = 0
    current = 0
    n = len(steps)
    # 아래의 소괄호 부분에 알맞은 조건을 적어 주세요
    while current < n:
        # 아래의 등호(=) 옆에 current에 토끼가 현재 밟고 있는 발판의 숫자를 더하는 코드를 적어 주세요
        current = current + steps[current]
        cnt = cnt + 1
    return cnt

print(cross_bridge([1, 1, 2, 3, 5]))



배열의 길이를 알아보는 함수는 len()이다.
